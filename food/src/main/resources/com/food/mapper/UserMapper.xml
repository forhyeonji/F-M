<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.food.mapper.UserMapper">
    <insert id="join">
        insert into tb_user(user_email, user_id, user_pw, user_name, user_nick, user_add, user_gender, user_birth, user_phone)
        values(#{user_email},#{user_id},#{user_pw},#{user_name},#{user_nick},#{user_add},#{user_gender},#{user_birth},#{user_phone})
    </insert>

    <select id="login" resultType="com.food.model.UserVO">
        select user_id, user_pw
        from tb_user
        where user_id = #{user_id} and user_pw = #{user_pw}
    </select>
</mapper>